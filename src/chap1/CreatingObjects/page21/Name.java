//OBJECTS
package chap1.CreatingObjects.page21;

public class Name {
    String first = "Theodore";
    String last = "Moose";
    String full = first + last;
}

/*Lines 2 and 3 both write to fields.
Line 4 both reads and writes data. It reads the fields first and last.
It then writes the field full.
 */
