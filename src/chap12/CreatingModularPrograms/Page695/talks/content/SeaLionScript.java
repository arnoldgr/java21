package zoo.animal.talks.content;

/**
 * SeaLionScript class
 * -------------------
 * Represents the script used for sea lion talks in the zoo.
 * This class is part of the `zoo.animal.talks.content` package,
 * which is explicitly exported in the module declaration.
 *
 * ✅ Important:
 * - Because the package is exported in `module-info.java`, other
 *   modules can access this class.
 * - This demonstrates how the `exports` directive makes selected
 *   packages available outside the module.
 * - Without the export, this class would only be usable internally
 *   within the zoo.animal.talks module.
 */
public class SeaLionScript {

    /**
     * Placeholder class
     * -----------------
     * Currently contains no fields or methods.
     * In a real application, this class could store dialogue,
     * educational content, or behavior scripts for sea lion talks.
     *
     * For exam purposes, the focus is on understanding how
     * packages and modules are structured and exported.
     */
}