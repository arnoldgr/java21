
package zoo.animal.talks.schedule;

/**
 * Weekday class
 * -------------
 * Represents the schedule for zoo talks held on weekdays.
 * This class is part of the `zoo.animal.talks.schedule` package,
 * which is explicitly exported in the module declaration.
 *
 * ✅ Important:
 * - Because the package is exported in `module-info.java`, other
 *   modules can access this class.
 * - This demonstrates how the `exports` directive makes selected
 *   functionality available outside the module.
 * - Currently a placeholder, but in a real application it could
 *   store details such as talk times, speaker assignments, or
 *   animal-specific schedules for weekdays.
 */
public class Weekday {
    // Placeholder class with no fields or methods
}