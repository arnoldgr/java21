//module zoo.visitor {
////    requires zoo.tours.api;           // Needed to know the Tour interface
//    requires zoo.tours.reservations;  // Needed to use TourFinder
//
//}
//// - No need for 'uses' here because this module is consuming, not providing.
//// - The consumer module depends on the API and the reservations module.